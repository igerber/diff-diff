"""diff-diff-specific bindings for the plan-review eval harness.

These adapters wire the plan corpus, the ``claude -p`` / codex reviewer
invocations, and detached-worktree materialization into the generic
``eval_core`` engine (shared with ``tools/reviewer-eval/``).
"""
